# تقييم Mesori قبل النشر

تقييم مبني على اللي شفته واتأكدت منه فعلياً وأنا شغال على الكود
وقاعدة البيانات الحية طول الجلسات اللي فاتت — مش تخمين عام. كل بند
مقيّم من ١٠، مع السبب المحدد ورا الرقم.

---

## ١. تصميم UI/UX — **٧/١٠**
هوية بصرية متماسكة (الذهبي/الرملي الفرعوني)، وتفاصيل مدروسة (تأثيرات
الطيف واللمعان على كروت المستويات، الشعار المتحرك). لكن: الصفحة
الرئيسية وصفحة الليدربورد كان فيهم أخطاء حقيقية (تمرير غير مقصود،
كروت غير متساوية، صورة متقصوصة) قبل المراجعة دي — يعني فيه فجوة كانت
موجودة بين التصميم المقصود والتنفيذ الفعلي. وفيه تعديل واحد على الأقل
(حجم اللوجو/الكروت) رجعت فيه بعد ملاحظتك، يعني التصميم لسه في طور
الاستقرار مش نهائي ١٠٠٪.

## ٢. Mobile-First / الاستجابة — **٧/١٠**
استخدام صحيح لـ `dvh` بدل `100vh`، `safe-area-inset`،
`viewport-fit=cover`. لكن لقيت باگ `100vh` قديم فات في شاشة التحميل
نفسها، والأهم: **ولا تعديل واحد من كل اللي اتعمل (من الجلسة اللي
فاتت ومن النهارده) اتجرّب فعلياً على جهاز أو محاكي حقيقي** — كل
التحقق كان حسابي/منطقي في الكود بس.

## ٣. جودة الكود والبنية — **٨/١٠**
تعليقات عربية مفصّلة وواضحة (بتشرح "ليه" مش بس "إيه") في كل ملف
تقريباً — ده أعلى من المتوسط بوضوح لمشروع فردي. فصل منطقي بين
pages/components/lib. في المقابل: بعض الملفات كبيرة جداً
(`VsMatchPage.jsx` بيحمل منطق كتير في ملف واحد)، مفيش TypeScript
(يعني أخطاء في شكل البيانات بتتكشف وقت التشغيل مش وقت الكتابة)، وحجم
الـ JS bundle الرئيسي تخطى ٦٣٠ كيلوبايت (Vite نفسه بيطلع تحذير بيه).

## ٤. قاعدة البيانات والأمان — **٦/١٠**
التصميم الأمني الأساسي كويس فعلاً (RLS بمنطق واضح، وview الليدربورد
مصمّم عمداً يستبعد أعمدة حساسة زي العمر والدولة). لكن اكتشفت النهارده
باگ حقيقي شغّال فعلياً (صلاحية SELECT ناقصة على `questions`، عطّلت
مود ١ ضد ١) — ولما رجعت لتاريخ المشروع، لقيت نفس فئة الباگ بالظبط
(RLS موجودة، الصلاحية الأساسية ناقصة) حصلت قبل كده مع جدول
`notifications` (migration 009). يعني ده مش حادثة منفردة، دي فجوة
متكررة في طريقة التحقق من الصلاحيات بعد أي migration.

## ٥. الأداء — **٦/١٠**
الصور اتظبطت كويس من قبل كده (WebP، ضغط اللوجو). لكن الـ bundle
الرئيسي للـ JS كبير (٦٣٤ كيلوبايت، ١٨٦ بعد الضغط) من غير أي
code-splitting — يعني VsMatchPage وQuizPage وLeaderboardPage كلهم
بينزلوا مع بعض حتى لو المستخدم فاتح صفحة الهوم بس. وملف خط الأيقونات
لوحده أكتر من ميجا. على نت موبايل عادي في مصر، ده وقت تحميل أول
فتحة محسوس.

## ٦. الاختبارات الآلية — **٤/١٠**
فيه ٥ اختبارات بس، بتغطي بوابة تسجيل الدخول والتنقل الأساسي بين
أسئلة الكويز. **مفيش أي اختبار** لمود ١ ضد ١ (أعقد جزء منطقياً في
التطبيق كله — presence، انسحاب، بوت)، ولا للیدربورد، ولا لدوال
السيرفر (`get_match_question`، `submit_match_answer`). لو حصل خطأ
منطقي في مود ١ ضد ١، مفيش حاجة هتمسكه قبل ما مستخدم حقيقي يوصله.

## ٧. إمكانية الوصول (Accessibility) — **٥/١٠**
تباين الألوان اتصلح قبل كده (WCAG)، ومشكلة تكبير iOS عند الكتابة في
الحقول اتصلحت. لكن فيه خطوط بحجم ثابت صغير جداً (٩-١٠ بكسل) في أماكن
كتير — بما فيهم حاجات ضفتها أنا النهارده في تصغير الكروت — وده
معروف من أول مراجعة ومتأجل عمداً. المشكلة دي أهم من العادي هنا لأن
المستخدمين المستهدفين فيهم أطفال في سن مدرسي.

## ٨. الجاهزية للنشر على Google Play — **٥/١٠**
التطبيق وظيفياً حاسس إنه مكتمل بشكل معقول (تسجيل دخول، كويز، مود ١
ضد ١، ليدربورد). لكن: مفيش مجلد `android/` لسه (يعني Capacitor
الأصلي متعملوش initialize خالص، صفر اختبار على جهاز حقيقي لحد
دلوقتي)، وحسب آخر مراجعة جاهزية كانت فيه بنود لسه محتاجة تأكيد
(AdMob، app ID). Google Play Families Policy (لأن التطبيق بيستهدف
قاصرين) محتاج مراجعة فعلية قبل الرفع، مش افتراض إنه تمام.

## ٩. المحتوى واللغة (RTL) — **٨/١٠**
عربي طبيعي ومتّسق في كل حتة (واجهة، تعليقات، رسائل خطأ) — مش حاسس
إنه ترجمة لاحقة. لكن باگ الليدربورد (صورة متقصوصة بسبب RTL) بيثبت
إن مشاكل RTL ممكن تفوت حتى في تطبيق مبني عربي من الأساس — وأنا
راجعت بعمق بس Home وLeaderboard وLogin؛ صفحات زي Profile وVsLobby
وQuizGroup لسه محتاجة نفس الفحص.

## ١٠. بنية المميزات اللحظية (Realtime/Backend) — **٨/١٠**
هندسة متقدمة فعلاً لحجم مشروع فردي: presence heartbeat للانسحاب
التلقائي، وضع بوت احتياطي، تعامل دقيق مع المباريات الملغاة. تاريخ
الـ migrations موثّق كويس ("ليه" مش بس "إيه"). العيب: التعقيد ده عبء
صيانة كبير لمطوّر واحد، وفئة باگ الصلاحيات (بند ٤) حصلت مرتين دلوقتي
في نفس النوع من العمليات.

---

## المعدّل الإجمالي: **٦.٤ / ١٠**

تطبيق بأساس تقني وتصميمي حقيقي وقوي، لكنه لسه محتاج شغل ملموس قبل
النشر — مش مجرد لمسات أخيرة.

## اقتراحاتي بالأولوية

1. **الأهم**: `npx cap add android` وجرّب على جهاز/محاكي حقيقي —
   كل التعديلات لحد دلوقتي (من قبل ومن النهارده) اتحقّق منها بالكود
   بس، صفر تأكيد بصري.
2. اعمل قائمة تأكيد رسمية لـ Google Play Families Policy بدل
   الافتراض، خصوصاً لو هتضيف إعلانات.
3. راجع أحجام الخطوط الثابتة الصغيرة (٩-١٠px) — دي مؤجلة من مراجعة
   قبل كده ولسه موجودة.
4. قسّم الـ JS bundle (`dynamic import()`) لصفحات VS mode/Quiz/
   Leaderboard على الأقل — هيقلّل وقت أول تحميل محسوس.
5. ضيف على الأقل اختبارات أساسية لمود ١ ضد ١ والليدربورد — أعقد
   وأهش جزء في التطبيق حالياً بدون أي غطاء اختبار.
6. اعتمد خطوة تحقق ثابتة بعد أي migration جديد: `GRANT` + RLS مع
   بعض، مش RLS لوحدها — نفس الفجوة حصلت مرتين بالفعل.
7. مرّر نفس فحص RTL اللي عملناه على Home/Leaderboard على باقي
   الصفحات (Profile, VsLobby, QuizGroup).
